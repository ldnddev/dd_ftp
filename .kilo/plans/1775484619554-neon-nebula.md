# dd_ftp Phase 5 - Cleanup and Quality Improvements

## Overview
This phase focuses on code quality, cleanup, and foundational improvements before adding new features (which are now deferred to Phase 6).

## Original Phase 5 Items Moved to Phase 6
The following features are deferred to Phase 6:
1. Filters
2. Directory comparison
3. Sync browsing
4. Proxy support
5. Logging expansion
6. Remote search

---

## Phase 5 Cleanup Tasks

### 0. Update ARCHITECTURE.md with New Phase Structure
**Priority: TRIVIAL (First)**
- Update Build Phases section to reflect:
  - Phase 5 = Cleanup and Quality Improvements
  - Phase 6 = Filters, Directory comparison, Sync browsing, Proxy, Logging expansion, Remote search
- Update Next Steps section to remove outdated items

### 1. Remove Unused Workspace Dependencies
**Priority: HIGH**
- Remove `serde_json` from workspace.dependencies (never used)
- Remove `parking_lot` from workspace.dependencies (never used)
- Verify no other unused dependencies exist

### 2. Clean Up Placeholder Protocol Modules
**Priority: MEDIUM**
- Remove `/home/jlyvers/projects/dd_ftp/crates/dd_ftp_protocols/src/ftp.rs` (implementation moved to dd_ftp_ftp)
- Remove `/home/jlyvers/projects/dd_ftp/crates/dd_ftp_protocols/src/ftps.rs` (implementation moved to dd_ftp_ftp)
- Update `dd_ftp_protocols/src/lib.rs` to remove module declarations

### 3. Remove or Implement Unused AppEvent Enum
**Priority: MEDIUM**
- The `AppEvent` enum in `dd_ftp_app/src/actions.rs` is exported but never used
- Decision needed: Either implement the intended event system or remove it
- If keeping: Document its purpose and integrate into the event loop
- If removing: Clean up exports in `dd_ftp_app/src/lib.rs`

### 4. Implement or Remove Placeholder Logging
**Priority: MEDIUM**
- `dd_ftp_utils/src/logging.rs` has empty `init_logging()` function
- Either implement proper tracing subscriber setup or remove the module
- If implementing: Add configurable logging levels and output destinations
- If removing: Clean up exports and any callers

### 5. Fix Clippy Warnings
**Priority: LOW**
- `if_same_then_else` in `dd_ftp_ui/src/render.rs:245-250` - merge conditions with `||`
- `useless_vec` in `dd_ftp_ui/src/render.rs:363-372` - use array instead of vec
- `field_reassign_with_default` in `dd_ftp_cli/src/main.rs:36-37` - use struct update syntax

### 6. Improve Error Handling
**Priority: MEDIUM**
- Replace `unreachable!()` in `dd_ftp_cli/src/main.rs:180` and `:700` with proper error handling
- Consider using `warn!` log or restructuring to make pattern match exhaustive

### 7. Optimize String Comparisons
**Priority: LOW**
- Replace `to_lowercase()` with `eq_ignore_ascii_case()` for ASCII-only comparisons
- Locations:
  - `dd_ftp_ui/src/render.rs:247-258` (8 uses)
  - `dd_ftp_cli/src/main.rs:980`
  - `dd_ftp_protocols/src/sftp.rs:121`

### 8. Add Documentation
**Priority: LOW**
- Add doc comments to public API items:
  - `dd_ftp_core/src/error.rs` - `CoreError` enum
  - `dd_ftp_core/src/connection.rs` - `Protocol`, `ConnectionInfo`
  - `dd_ftp_core/src/traits.rs` - `RemoteSession` trait
  - `dd_ftp_core/src/transfer.rs` - `TransferDirection`, `TransferStatus`, `TransferJob`
  - `dd_ftp_storage/src/secrets.rs` - `SecretStore` methods

### 9. Code Style Cleanup
**Priority: TRIVIAL**
- Remove trailing comma after function body in `dd_ftp_cli/src/main.rs:369`
- Remove outdated TODO comment in workspace `Cargo.toml:56`

### 10. Add Unit Tests
**Priority: HIGH**
Add test coverage for critical modules:
- `dd_ftp_core`: TransferJob creation, EntryKind handling, ConnectionInfo default
- `dd_ftp_app`: State mutations via reducer
- `dd_ftp_transfer`: Queue operations (enqueue, start_next, mark_completed/failed/cancelled)
- `dd_ftp_storage`: SiteManager TOML serialization, password key generation
- `dd_ftp_ui`: Theme parsing, progress formatting, path shortening

---

## Execution Order

1. **First Pass - Quick Wins**
   - Task 0: Update ARCHITECTURE.md with new phase structure
   - Task 9: Code style cleanup (trivial)
   - Task 1: Remove unused dependencies
   - Task 5: Fix clippy warnings
   - Task 7: Optimize string comparisons

2. **Second Pass - Structural Cleanup**
   - Task 2: Remove placeholder protocol modules
   - Task 3: Resolve AppEvent enum (decide keep/remove)
   - Task 4: Resolve logging placeholder (implement/remove)
   - Task 6: Improve error handling

3. **Third Pass - Documentation and Testing**
   - Task 8: Add documentation
   - Task 10: Add unit tests

---

## Success Criteria

- [ ] ARCHITECTURE.md updated with Phase 5/6 split
- [ ] `cargo check` passes with no warnings
- [ ] `cargo clippy` passes with no warnings
- [ ] All placeholder/empty modules removed or implemented
- [ ] No unused dependencies in workspace
- [ ] Public API has basic documentation
- [ ] Core modules have unit test coverage
- [ ] No `unreachable!()` or `panic!` in production paths

---

## Notes

- Each task should be a separate commit for clean history
- Run `cargo check` and `cargo clippy` after each task
- Consider adding `cargo clippy` to CI if not already present
